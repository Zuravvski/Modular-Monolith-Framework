name: Publish on new changes

on: [workflow_dispatch]

jobs:
  test:

    runs-on: ubuntu-latest

    steps:
    - name: Output Run ID
      run: echo ${{ github.run_id }}
    - name: Output Run Number
      run: echo ${{ github.run_number }}
